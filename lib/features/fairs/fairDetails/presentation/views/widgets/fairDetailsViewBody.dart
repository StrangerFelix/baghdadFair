import 'package:flutter/material.dart';

class FairDetailsViewBody extends StatelessWidget {
  const FairDetailsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      
    );
  }
}